# @Valid 注解的使用
在http请求中，我们经常需要对参数进行校验，比如非空，长度，正则等。但有如下注意点：
* 请求体参数前必须加 **@RequestBody** 注解和 **@Valid** 注解，否则校验不生效
* 校验失败会抛出 **MethodArgumentNotValidException** 异常，需要捕获处理，
用 **@ControllerAdvice** 注解和 **@ExceptionHandler** 注解处理。
* 如果请求体内部参数有对象或者集合，需要对对象或者集合进行校验，需要加上
**@Valid** 注解，否则校验不生效


